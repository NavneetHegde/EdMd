<!-- No StartupUri: App.OnStartup creates the window itself, but only after the
     single-instance check (a second launch forwards its files to the running window). -->
<Application x:Class="EdMd.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
</Application>
